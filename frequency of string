#Write code to Calculate frequency of characters in a string


str = input("enter str:")
freq = {}

for ch in str:
    if ch in freq:
        freq[ch]+=1
    else:
        freq[ch]=1
for ch,count in freq.items():
    print(f"{ch}:{count}")
